Card091 runner bundle — CPU preparation, no GPU authority

Root protocol and quality definitions live in overseer-codex/card091-full-reference-seeds.md and card091-quality-prereg.md. All four090 endpoints are required regardless of scientific PASS. No historical projection inputs are copied. Original078 projection/sensor/checkpoint/stream primitives are preserved byte for byte locally; all basemap files remain original090 bytes.

Future root sequence (do not execute absent root release/preparation scheduling):
1. After all four090 canonical endpoints exist, prepare_card091_selection.py validates actual original090 payloads in a separate CPU process, freezes all model/prepared/admission/manifest/validation/snapshot/retained-checkpoint hashes, and writes O/card091-full/selection.json plus selection-build.json. Existing selection is never overwritten. It performs no model forward or raw-feature hashing. It does verify reference and endpoint file hashes. Its measured CPU time must be charged within root's ONE1200 CPU allocation; implementation tests are not execution admission.
2. suggest_card091_release.py merges proposed source/instrument bindings with actual completed inputs, verifies them, and emits a CANDIDATE ONLY at O/card091-full/release-candidate.json. It never writes root release. Its CPU wall time is exposed for the same single allocation. Root owns scorer/auditor/handoff, generated-input freeze and scheduling.
3. Only root can create O/card091-release.json and start run_card091_chain.py under BOTH actual external flock wrappers, 32GiB/no-swap. The chain starts occupancy before initial verify/lease inspection, journals one conservative reservation, monitors the full child process tree/global VRAM, and settles every exit. No internal GPU lock acquisition or notifications.
4. Released GPU producer invokes the actual original090 loader in an isolated subprocess, verifies all four states and FP32 outputs against the chosen unchanged091 inference core on fixed1024 reference rows, runs original fidelity and sensor checks, buffering parity/timing, real checkpoint reopen plus wrong-identity controls, then six-position full-path four-head preflight. The stream covers exact original split-aligned chunks; successful terminal requires all103816750 rows and eight primary arrays. Partial/orphan outputs are not accepted or silently overwritten.

Joint admission uses canonical ledger less the active reservation plus current actual elapsed time, preserving prior attempts and concurrent journal adjustments. Estimate = worst of six complete chunk timings * exact remaining split-aligned chunk count *1.2 +240s hashing/resume/closeout reserve. This extra conservative engineering reserve does not change the3600cap. It must fit both card/window and now+max(estimate, remaining090CPU bound)+1200 <=23:50:55. Not-yet-started090CPU reserves1800; active090CPU must have current launch/release deadline and enforced16GiB/no-swap; stale, failed-launch, expired or unexplained inactive states stop for root. Root091CPU must wait for090CPU inactivity and use ONE absolute deadline, with all freeze/wait/hash/score/audit costs included. No runner CPU scorer or CPU timer is created.

Terminal O/card091-full/execution.json and O/card091-execution.json expose PASS, status PROJECTED_NOT_SCORED, rows, ordered models ranked43/uniform43/ranked44/uniform44, eight output hashes, inactive counts/histogram/ID/query hashes, runtime/selection/release/progress/producer hashes, FP32=True, TF32=False, head_batch=256 and resource measurements. Root must also require card091-chain-execution.json PASS because post-producer verification/accounting can still fail. Fresh loader, buffer, canary, preflight and terminal receipts bind current runtime/selection/release and must be newer than stage start.

The inherited078 wrong-row test assumed varied outputs. Card091 instead rejects reversed fixed probe IDs explicitly, even for constant outputs, and records constant probe outputs without a quality-based omission. Numeric tolerance is unchanged. CPU fixtures cover constant outputs and wrong ID/precision/state/value rejection. Actual device equality/fidelity is pending.

CPU verification: test_card091_contracts.py uses tiny synthetic arrays and temporary ledgers/payloads, mocked subprocesses for actual selection/controller success and failure paths, and the real checkpoint primitive. It does not run a production model, feature scan or GPU operation. Runtime SHA manifest excludes generated outputs and this explanatory README; source-contracts and cpu-contracts are separate readiness evidence.

Timing correction: every one of the six preflight windows includes checkpoint flush, CUDA synchronization and recurring resources() ps/nvidia-smi overhead before elapsed capture. Focused AST/mock-clock controls include the old-order negative control; previous unrelated suites are inherited, not rerun.
